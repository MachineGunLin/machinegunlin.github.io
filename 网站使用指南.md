# 树哥的地下室：网站使用指南

这份指南用于日常维护“树哥的地下室”，包括用 VS Code 写 Markdown、管理文章和图片、本地预览，以及把内容发布到 GitHub Pages。

## 一、先记住这几个位置

项目目录：

```text
/Users/linrongjian/Desktop/ship_it/machinegunlin.github.io
```

常用文件：

```text
machinegunlin.github.io/
├── src/content/posts/zh-CN/   # 中文文章
├── src/content/posts/en/      # 英文文章
├── public/images/posts/       # 建议存放文章图片
├── src/pages/about.astro      # 关于页
├── src/site.config.ts         # 网站名称、简介等全局信息
├── src/styles/global.css      # 全站样式
├── 网站使用指南.md
└── README.md
```

文章文件名会成为网址的一部分。例如：

```text
src/content/posts/zh-CN/how-i-use-ai.md
```

发布后的地址是：

```text
https://machinegunlin.github.io/posts/how-i-use-ai/
```

文件发布后尽量不要改名，否则旧链接会变成 404。

## 二、第一次在 VS Code 中打开项目

在终端运行：

```bash
cd /Users/linrongjian/Desktop/ship_it/machinegunlin.github.io
code .
```

如果 `code` 命令不可用，也可以打开 VS Code，选择“文件 → 打开文件夹”，再选择 `machinegunlin.github.io`。

第一次使用时安装依赖：

```bash
npm install
```

项目要求 Node.js 22.12 或更高版本。可以这样查看版本：

```bash
node --version
```

`npm install` 通常只需要在第一次使用、切换电脑或依赖发生变化后执行。

## 三、新建一篇中文文章

### 1. 用脚本生成文章

在项目目录运行：

```bash
npm run new:post -- "文章标题" zh-CN
```

例如：

```bash
npm run new:post -- "我怎样使用 AI 写代码" zh-CN
```

脚本会在 `src/content/posts/zh-CN/` 下创建 Markdown 文件，并默认设置：

```yaml
draft: true
```

这意味着文章还是草稿，不会出现在网站、RSS、分类页或标签页中。

### 2. 给中文文章改一个稳定的英文文件名

纯中文标题生成的默认文件名通常类似 `post-2026-08-31.md`。建议创建后立刻在 VS Code 中重命名为简短的英文 kebab-case：

```text
how-i-use-ai.md
```

文件名建议遵守以下规则：

- 只使用小写英文字母、数字和短横线
- 不使用空格、中文、下划线或特殊符号
- 尽量简短，同时能看出文章主题
- 发布后不要随意改名

如果同一天新建多篇中文文章，必须先重命名第一篇，否则生成器会因为文件名重复而拒绝创建下一篇。

### 3. 填写文章信息

每篇文章开头都有一段 Frontmatter：

```yaml
---
title: 我怎样使用 AI 写代码
description: 记录我在独立开发中使用 AI 编程工具的实际方法、限制和判断。
publishDate: 2026-08-31
updatedDate: 2026-08-31
tags:
  - AI
  - 独立开发
category: AI 与技术
lang: zh-CN
translationKey: how-i-use-ai
draft: true
---
```

各字段含义：

| 字段 | 用途 | 注意事项 |
| --- | --- | --- |
| `title` | 文章标题 | 必填 |
| `description` | 摘要和搜索结果描述 | 必填，建议用一两句话说清文章内容 |
| `publishDate` | 首次发布日期 | 使用 `YYYY-MM-DD` |
| `updatedDate` | 最近修改日期 | 修改正文后同步更新；不需要时可以删除这一行 |
| `tags` | 标签 | 可以有多个，也可以写成 `tags: []` |
| `category` | 主分类 | 只能使用下方四个固定分类之一 |
| `lang` | 文章语言 | 中文用 `zh-CN`，英文用 `en` |
| `translationKey` | 中英文关联键 | 一般与英文文件名一致；翻译版必须使用同一个值 |
| `draft` | 是否为草稿 | `true` 不上线，`false` 才会发布 |

目前允许的分类只有：

```text
AI 与技术
数学与学习
独立开发
思考与生活
```

分类文字必须完全一致，否则构建检查会失败。

### 4. 开始写 Markdown

Frontmatter 下方就是正文：

```markdown
这是一段正文。

## 二级标题

这是另一个段落。

### 三级标题

- 第一项
- 第二项

> 这是一段引用。

[链接文字](https://example.com/)
```

二级和三级标题会自动进入文章目录。

在 VS Code 中按 `Command + Shift + V` 可以快速打开 Markdown 预览。它适合检查标题、列表和段落，但不完全等于网站最终效果。特别是 `/images/...` 形式的站点图片路径，在 VS Code 自带预览中可能无法显示；图片和样式应以 `npm run dev` 打开的网页为准。

## 四、插入图片

### 推荐的图片目录

每篇文章使用一个与文章文件名相同的图片文件夹：

```text
public/images/posts/how-i-use-ai/
```

完整示例：

```text
src/content/posts/zh-CN/how-i-use-ai.md
public/images/posts/how-i-use-ai/workflow.webp
public/images/posts/how-i-use-ai/result.png
```

可以直接在 VS Code 左侧文件树中：

1. 展开 `public` 文件夹。
2. 创建 `images/posts/how-i-use-ai` 文件夹。
3. 把图片拖进这个文件夹。
4. 把图片重命名为小写英文文件名。

### 在 Markdown 中引用图片

```markdown
![AI 编程工作流示意图](/images/posts/how-i-use-ai/workflow.webp)
```

注意：

- 路径从 `/images/` 开始，不要写 `public`
- 图片说明不要留空，它会帮助读者和屏幕阅读器理解图片
- 文件名不要包含空格、中文或特殊符号
- 路径和文件名区分大小写

如果需要显示图注，可以使用 HTML：

```html
<figure>
  <img src="/images/posts/how-i-use-ai/workflow.webp" alt="AI 编程工作流示意图" />
  <figcaption>我目前使用的写作与开发流程。</figcaption>
</figure>
```

### 图片格式和大小建议

- 照片优先使用 WebP 或 JPEG
- 截图、界面和需要透明背景的图片使用 PNG 或 WebP
- 正文图片宽度通常不需要超过 1600 像素
- 普通图片尽量控制在 500 KB 以内
- 发布前确认自己拥有图片使用权，并注明必要的来源

可以在 macOS 中查看图片大小：

```bash
du -h public/images/posts/how-i-use-ai/*
```

使用系统自带的 `sips` 把图片最长边缩到 1600 像素：

```bash
sips -Z 1600 original.jpg --out public/images/posts/how-i-use-ai/workflow.jpg
```

这条命令会生成新文件，不会自动替换原图。先确认输出效果，再决定是否删除原图。

## 五、本地预览文章

在项目目录运行：

```bash
npm run dev
```

终端会显示本地地址，通常是：

```text
http://localhost:4321/
```

按住 Command 点击终端里的地址，或者复制到浏览器打开。

开发服务器运行时，保存 Markdown 文件后页面会自动刷新。结束预览时，在终端按 `Control + C`。

### 怎样预览草稿

当前网站会过滤所有 `draft: true` 的文章，本地开发环境也一样。预览草稿时可以暂时改成：

```yaml
draft: false
```

在真正准备发布之前，如果文章还没有写完，再改回：

```yaml
draft: true
```

不要只看首页，还要打开文章详情页，重点检查：

- 标题、摘要、日期、分类和标签是否正确
- 图片是否加载，替代文字是否准确
- 标题层级和目录是否合理
- 代码块、表格和数学公式是否溢出
- 手机宽度下是否容易阅读
- 文中的链接是否指向正确页面

## 六、发布文章到网站

### 1. 发布前处理

对外文章定稿前，按项目规则使用 `humanizer-zh` 完整处理一遍，但不要改变事实、链接、引用或作者观点。

确认 Frontmatter 中：

```yaml
updatedDate: 2026-08-31
draft: false
```

日期替换为实际日期。

### 2. 运行完整检查

先停止开发服务器，再运行：

```bash
npm run verify
```

只有在检查全部通过后才继续发布。这个命令会检查：

- Astro 和 TypeScript
- Frontmatter 字段
- Markdown、代码块和数学公式
- 生产构建
- 内部链接
- RSS 和 Sitemap
- 404、隐私和打印样式

如果检查失败，不要强行提交。根据终端显示的文件名和错误信息修改后，再重新运行。

### 3. 查看本次准备提交的内容

```bash
git status --short
git diff
```

确认列表中只有这次文章、图片和你有意修改的文件。不要提交私人笔记、临时截图、密钥或 `.env`。

### 4. 提交并推送

假设文章是 `how-i-use-ai.md`，图片在同名目录：

```bash
git add src/content/posts/zh-CN/how-i-use-ai.md
git add public/images/posts/how-i-use-ai/
git commit -m "content: publish how I use AI"
git push origin main
```

建议明确添加文件，不要习惯性使用 `git add .`，这样更不容易把无关内容传到公开仓库。

推送后，GitHub Actions 会自动构建并部署，一般需要一两分钟。部署状态：

```text
https://github.com/MachineGunLin/machinegunlin.github.io/actions
```

工作流显示绿色的 `Success` 后，打开正式文章地址检查：

```text
https://machinegunlin.github.io/posts/how-i-use-ai/
```

首页、文章列表、分类页、标签页、RSS 和 Sitemap 都会由构建流程自动更新，不需要手工维护。

## 七、文章的增删改查

### 增：新建文章

中文文章：

```bash
npm run new:post -- "文章标题" zh-CN
```

英文文章：

```bash
npm run new:post -- "English post title" en
```

然后完成写作、图片、本地检查、`draft: false`、commit 和 push。

### 查：查找文章

在 VS Code 中：

- 按 `Command + P`，输入部分文件名打开文章
- 按 `Command + Shift + F`，按标题、标签、正文关键词或 `translationKey` 搜索
- 在左侧文件树查看 `src/content/posts/zh-CN/` 和 `src/content/posts/en/`

也可以在终端查找：

```bash
rg "AI" src/content/posts
```

列出所有文章文件：

```bash
rg --files src/content/posts | sort
```

### 改：修改已发布文章

1. 在 VS Code 中修改原 Markdown 文件。
2. 把 `updatedDate` 改为实际修改日期。
3. 运行 `npm run dev` 检查页面。
4. 运行 `npm run verify`。
5. 查看 `git diff`。
6. 提交并推送。

示例：

```bash
git add src/content/posts/zh-CN/how-i-use-ai.md
git commit -m "content: update AI workflow article"
git push origin main
```

不要为了修改标题而顺便改文件名。修改 `title` 不会改变网址，改文件名会改变网址。

### 删：隐藏或删除文章

如果只是暂时下线，推荐把文章改成：

```yaml
draft: true
```

这样文件和历史仍然保留，但文章不会出现在网站和 RSS 中。完成检查后提交：

```bash
git add src/content/posts/zh-CN/how-i-use-ai.md
git commit -m "content: unpublish AI workflow article"
git push origin main
```

如果确定永久删除，可以运行：

```bash
git rm src/content/posts/zh-CN/how-i-use-ai.md
git rm -r public/images/posts/how-i-use-ai/
npm run verify
git commit -m "content: remove AI workflow article"
git push origin main
```

删除前确认图片文件夹没有被其他文章引用。文章删除后，原网址会返回 404，外部已经传播的链接也会失效。

## 八、中英文文章怎么关联

中文文章放在：

```text
src/content/posts/zh-CN/
```

英文文章放在：

```text
src/content/posts/en/
```

两篇互为翻译的文章必须使用相同的 `translationKey`：

```yaml
translationKey: how-i-use-ai
```

同时分别设置语言：

```yaml
lang: zh-CN
```

```yaml
lang: en
```

发布后地址分别是：

```text
https://machinegunlin.github.io/posts/how-i-use-ai/
https://machinegunlin.github.io/en/posts/how-i-use-ai/
```

只有两篇文章都设置为 `draft: false` 时，页面才会显示互相切换语言的链接。

## 九、代码、数学公式和表格

代码块：

````markdown
```ts
const message: string = 'hello';
```
````

行内公式：

```markdown
质能方程是 $E = mc^2$。
```

独立公式：

```markdown
$$
\sum_{k=1}^{n} k = \frac{n(n+1)}{2}
$$
```

表格：

```markdown
| 项目 | 状态 |
| --- | --- |
| 本地检查 | 完成 |
| 正式发布 | 待处理 |
```

这些内容已经由网站统一处理，不需要额外引入脚本或在线服务。

## 十、修改网站其他内容

修改关于页：

```text
src/pages/about.astro
```

修改网站名称、简介、作者和网址：

```text
src/site.config.ts
```

修改全站样式：

```text
src/styles/global.css
```

这类修改可能影响所有页面。修改后必须运行：

```bash
npm run verify
```

同时至少检查首页、文章页、关于页、404、桌面端和移动端。

## 十一、撤销错误操作

### 还没有 commit

先查看改动：

```bash
git status --short
git diff
```

如果只是 VS Code 中刚改错了一小段，优先使用编辑器的撤销功能，不要直接执行会覆盖全部修改的 Git 命令。

### 已经 commit 或已经上线

查看历史：

```bash
git log --oneline
```

使用 `git revert` 创建一个反向提交，比重写公开仓库历史更安全：

```bash
git revert <需要撤销的提交 ID>
git push origin main
```

如果不确定应该撤销哪个提交，先停止操作并检查 `git log` 和 GitHub 上的 commit 内容。

## 十二、建议的日常写作流程

每次发布可以照着执行：

1. 在 VS Code 中打开项目。
2. 用 `npm run new:post` 创建草稿。
3. 立即把文件改成稳定的英文文件名。
4. 填好标题、摘要、分类、标签和日期。
5. 用 Markdown 写正文，把图片放到文章同名图片目录。
6. 本地暂时设置 `draft: false`，运行 `npm run dev` 预览。
7. 继续写作时把文章恢复成 `draft: true`。
8. 定稿前完成事实、链接、图片版权和 `humanizer-zh` 检查。
9. 把 `updatedDate` 改为当天，把 `draft` 改为 `false`。
10. 运行 `npm run verify`。
11. 用 `git status --short` 和 `git diff` 检查公开内容。
12. 只 `git add` 本次文章及其图片。
13. commit 并 push 到 `main`。
14. 等待 GitHub Actions 成功。
15. 打开正式文章地址，再检查一次正文和图片。

## 十三、最常用命令速查

```bash
# 进入项目
cd /Users/linrongjian/Desktop/ship_it/machinegunlin.github.io

# 启动本地预览
npm run dev

# 新建中文草稿
npm run new:post -- "文章标题" zh-CN

# 新建英文草稿
npm run new:post -- "English post title" en

# 发布前完整检查
npm run verify

# 查看改动
git status --short
git diff

# 提交指定文章和图片
git add src/content/posts/zh-CN/example.md
git add public/images/posts/example/
git commit -m "content: publish example article"
git push origin main
```

正式网站：<https://machinegunlin.github.io/>

部署状态：<https://github.com/MachineGunLin/machinegunlin.github.io/actions>
